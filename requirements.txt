# Requisitos para la API de Tareas con Flask
#para instalar usar: pip install -r requirements.txt
# Núcleo API Flask
Flask==2.3.3
Werkzeug==2.3.7
flask-cors==4.0.0
flask-sqlalchemy==3.0.5
flask-jwt-extended==4.5.2
flask-restx==1.1.0
aniso8601>=9.0.1
jsonschema>=4.17.3

# Extras
markupsafe==2.1.3

# Consumo de APIs y configuración
requests==2.31.0
python-dotenv==1.0.0

# Despliegue (opcional, para Render/Railway/Docker)
gunicorn==21.2.0

# Tests y calidad (opcionales)
pytest
